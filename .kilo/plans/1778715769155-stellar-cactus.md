# Plan: Overhaul Playground CSS Theme and Stat Chips

## Goal
Overhaul the playground CSS color theme and style to use Obsidian non-purple dark mode (like VS Code dark theme) with the accent color of Solve (`#29ce99e8`) and appropriate background green colors. Additionally, update the stat chips to be taller with the spark line displayed below and the average line as a dotted line. Finally, move the header to a dark mode banner at the top.

## Current State
- Location: `playground/styles/main.css`
- Current Theme: Custom dark purple-ish theme (Catppuccin Mocha-inspired)
- Current Accents: Blue, Green, Red, Yellow, Purple, Cyan
- Current Backgrounds: `#1e1e2e`, `#252535`, `#2d2d3d`
- Stat chips: Current height is minimal, spark line is inline, average line is dashed in SVG

## Changes Required

### 1. Update CSS Variables (`:root`)
Replace the current color palette with VS Code dark mode inspired colors:
- **Backgrounds**: `#1e1e1e` (primary), `#252526` (secondary), `#2d2d2d` (tertiary)
- **Text**: `#cccccc` (primary), `#858585` (secondary)
- **Borders**: `#3c3c3c`

### 2. Update Accent Colors
- **Primary Accent**: `#29ce99e8` (Solve teal/green)
  - Use this for primary interactive elements (buttons, active states, headers)
  - Replace blue accents with variations of this color
- **Secondary Accents**: Derive from primary accent
  - Green: Variants of `#29ce99e8`
  - Error/Red: Keep or adjust to `#f48771` (VS Code red)
  - Yellow/Warning: Keep or adjust to `#dcdcaa` (VS Code yellow)
  - Purple: Replace with darker variant of teal/green or remove
  - Cyan: Replace with lighter variant of teal/green or remove

### 3. Update Specific Elements
- **Editor**: Update CodeMirror colors to match VS Code dark theme
- **Tokens/Tags**: Update token colors to use the new accent palette
- **Sidebar**: Update sidebar header background to use green background color
- **Buttons**: Update run button to use primary accent color
- **Results**: Update result backgrounds to use appropriate green shades

### 4. Update Stat Chips
- **Height**: Make stat chips taller to accommodate spark line below
- **Spark Line**: Ensure spark line displays below the text content (using `margin-top: auto`)
- **Average Line**: Verify average line is dotted in the SVG (already `stroke-dasharray="2,2"`)
- **Flex Layout**: Ensure the editor column flexes vertically so the editor fills remaining space and chips don't overflow off-screen (using `overflow-y: auto` on the editor section)

### 5. Move Header to Banner
- Move the `<header>` element from inside `.editor-column` to be a direct child of `<body>`
- Style the header as a full-width dark mode banner (`.app-banner`)
- Update `body` to use `flex-direction: column` so the banner sits at the top
- Update `.container` to `flex: 1` so it fills the remaining vertical space

### 6. File Changes
- `playground/index.html`: Structure changes
- `playground/styles/main.css`: Main source file
- `playground/dist/assets/index-*.css`: Built output (will be regenerated)

## Implementation Steps

1.  Read `playground/styles/main.css` and `playground/index.html` to understand current structure
2.  Update `:root` variables with new color palette
3.  Update all element styles to use new variables
4.  Update stat chip styles to be taller and adjust spark line positioning
5.  Move header to top-level banner and update layout CSS
6.  Test the changes by running the playground
7.  Verify the theme matches Obsidian/VS Code dark mode expectations

## Verification
- Start the playground server
- Check that the theme looks like Obsidian dark mode
- Verify that `#29ce99e8` is used as the primary accent
- Ensure all UI elements are readable and have proper contrast
- Verify stat chips are taller and spark line displays below with average dotted line
- Verify the header is now a banner at the top of the screen